# 使用手册

LCSC 元件一键导出工具 —— 安装与使用指南。

输入立创商城元件编号 → 导出 Altium `.SchLib` + `.PcbLib` + `.step`（无底座）。

---

## 一、部署到一台新电脑（零基础环境）

这个工具是一个**自包含的工作区文件夹**——除了 **Python 运行时**，所有依赖（PySide6、npnp.exe）都已打包在文件夹内的 `.tools/` 里，**不需要 pip 安装任何东西**。

一台什么都没装的新电脑，只需 **3 步**：

### 第 1 步：安装 Python 3.10+
1. 打开 <https://www.python.org/downloads/>，下载并安装 **Python 3.10 或更高版本**（本机用 3.14 验证过）。
2. **安装时务必勾选 "Add python.exe to PATH"**（安装界面底部的复选框）。
   - 很关键：它让**双击 `.pyw` 文件**和**命令行 `python`** 都能用。没勾的话，双击可能没反应、命令行找不到 `python`。

### 第 2 步：把整个工作区文件夹拷到新电脑
- 把**整个 `lcsc2altium` 文件夹**拷过去（U 盘 / 网盘 / 内网共享均可），约 **150 MB**（大头是 `.tools/pylibs` 里的 PySide6）。
- 放到任意位置都行；**路径里尽量别用中文和空格**（建议纯英文路径，如 `D:\lcsc2altium`），最稳妥。

### 第 3 步：运行（二选一）
- **双击** 文件夹里的 `lcsc2altium_gui.pyw`（弹出 GUI，无控制台窗口）；或
- 在该文件夹里开终端（cmd / PowerShell），执行：
  ```powershell
  python -m lcsc_exporter.app
  ```

> GUI 启动时会自动把 `.tools/pylibs` 加入 Python 路径、自动定位 `.tools/bin/npnp.exe`，无需任何手动配置。
>
> **需要联网**：npnp 在线抓取立创商城数据，首次每个编号约数秒~数十秒，之后走缓存会快很多。

---

## 二、使用步骤

1. **启动**：`python -m lcsc_exporter.app` 或双击 `lcsc2altium_gui.pyw`。
2. **输入编号**：在"LCSC 编号"框填立创商城编号，如 `C25161`。
   - 多个编号：用**空格 / 逗号 / 换行**分隔，如 `C25161 C8734 C25829`。
3. **选输出目录**：默认 `out`，可点"浏览…"更改。
4. **选导出目标**（下拉单选，选了哪种就直接出哪种，开箱即用）：
   - **Altium Designer**（默认）：`.SchLib` 原理图库 + `.PcbLib` 封装库（**3D 已内嵌**）+ 独立 `.step`。
   - **KiCad**：`.kicad_sym` 符号库 + `{型号}.pretty/` 封装库（`.kicad_mod` + **绑定 3D 的 STEP**）。
   - **强制重新抓取数据**：默认不勾。勾选则忽略缓存、重新联网抓取（数据更新或之前失败时用）。
5. **点"开始导出"**：后台逐个抓取并生成文件，进度条 + 日志实时显示，结果列表逐项汇报。
6. **打开输出目录**：点"打开输出目录"按钮，或在文件管理器打开 `out/{型号}/`。

---

## 二补、AI 选型助手（「AI 助手」页签）

不确定用什么型号时，先用大模型帮你选：

1. **配置**（仅首次，**每人用自己的 Key**，本工具不含任何 Key）：
   - 在「AI 助手」页签点「**申请 Key**」按钮，跳到服务商控制台注册领取（阿里云百炼/DeepSeek 都有免费额度）；
   - 把领到的 `sk-...` 粘贴进 API Key 框 → 点「保存配置」。
   - 端点预置：阿里云百炼（qwen-plus / deepseek-v3）、DeepSeek 官方（deepseek-chat），也可手填任何 OpenAI 兼容地址。
   - Key 只保存在你自己电脑的 `ai_config.json`（已加入 .gitignore，不会上传 GitHub）。
2. **描述需求**：如「3.3V LDO，SOT-23，低静态电流，给 3.7V 锂电用」，回车发送。
3. **AI 推荐 + 自动核验**：AI 给出候选型号后，工具**自动把每个型号拿到立创商城核验**：
   - ✅ 真实存在 → 显示立创型号和 **C 编号**（如 `XC6206P332MR-G（C5446）`）
   - ❌ 立创未找到 → 明确标注"建议不要采用"（大模型有时会编造型号，以此为准）
4. **一键去导出**：核验有通过项时，点「⬇ 把 N 个已核验编号填入元件导出」按钮——编号自动填进导出页，直接点「开始导出」即可。

---

## 三、输出说明

每个元件一个目录 `out/{型号}/`（型号即元件 MPN，如 `out/STM32F103C8T6/`；型号含非法字符时替换为 `_`，目录冲突时回退为 `{型号}_{编号}`），内含：

**Altium Designer 目标** —— `out/{型号}/`：

| 文件 | 说明 |
|------|------|
| `{name}.SchLib` | 原理图库（name 为 MPN，如 `0402WGJ0330TCE`） |
| `{封装}.PcbLib` | PCB 封装库（封装名，如 `R0402`），**3D 已内嵌** |
| `{封装}.step` | 独立 3D STEP（无底座，单位 mm，也可在 FreeCAD 等打开） |

**KiCad 目标** —— `out/{型号}/`：

| 文件/目录 | 说明 |
|-----------|------|
| `{型号}.kicad_sym` | 符号库（Footprint 属性已指向封装，自动关联） |
| `{型号}.pretty/` | 封装库目录（KiCad 的封装库就是一个 `.pretty` 文件夹） |
| `{型号}.pretty/{封装}.kicad_mod` | 封装 |
| `{型号}.pretty/{封装}.step` | 3D 模型，与 `.kicad_mod` 同目录**自动绑定** |

> 只生成所选目标的文件，无中间产物。

**导入 Altium**：
- 原理图：`Place → Part` 或原理图库管理器 → 导入 `.SchLib`。
- PCB：封装管理器 / `Place → Footprint` → 导入 `.PcbLib`。
- 3D：`.PcbLib` 已内嵌模型，PCB 3D 模式直接可见。

**导入 KiCad**：
- 符号：`设置(Preferences) → 管理符号库(Manage Symbol Libraries) → 添加现有库`，选 `{型号}.kicad_sym`。
- 封装：`设置 → 管理封装库(Manage Footprint Libraries) → 添加现有库`，选 `{型号}.pretty` 目录（昵称保持默认=目录名，这样符号里的 Footprint 属性能自动对上）。
- 3D：STEP 与 `.kicad_mod` 同目录，打开封装的 3D 预览即可见，无需设置。

---

## 四、常见问题

| 问题 | 处理 |
|------|------|
| **首次导出较慢** | npnp 需联网抓取立创数据，首次每编号约数秒~数十秒；批量时逐个进行，属正常。 |
| **报"找不到 npnp"** | 确认 `.tools/bin/npnp.exe` 存在；GUI 用 `find_npnp()` 自动定位（优先 `.tools/bin/`），路径异常时检查该文件。 |
| **某编号报错** | 多为该编号无对应封装/3D 模型，或临时网络问题。结果列表会标"失败"并给原因；可单独重跑该编号（勾"强制重新抓取"）。 |
| **418 / 请求被拒** | 接口高频触发反爬，稍等片刻重试；批量时程序已内置限速。 |
| **文件被占用（io error 32）** | 输出文件正被 Altium/资源管理器打开，先关闭再导出；或改输出目录。 |
| **STEP 有底座吗** | 无。直接采用立创标准库原生模型（已验证无底座）。 |

---

## 五、依赖与目录

```
├── lcsc2altium_gui.pyw     # 双击启动
├── lcsc_exporter/app/   # GUI 代码（调用 npnp）
├── out/                 # 导出产物
└── .tools/
    ├── bin/npnp.exe     # 导出引擎
    └── pylibs/          # PySide6
```

运行需：**Python 3.10+** + **PySide6**（`.tools/pylibs/`）+ **npnp.exe**（`.tools/bin/`）+ **联网**。

**内核归属**：导出内核 `npnp.exe` 是开源工具 [yycx2016/npnp](https://github.com/yycx2016/npnp)（**Apache-2.0**，纯 Rust，作者 yycx2016）；本工具的 GUI 壳为自研。分发时请保留 npnp 的 Apache-2.0 归属，详见 `README.md`「内核与开源归属」。
